program = whitespace? statements? whitespace?
statements = statement (whitespace statement)*
statement = comment / string / number / command
command = identifier

string = ~'"([^\"]|\\")*"'
number = ~"-?\d+(\.\d+)?"

identifier = ~"[^\d\s\".]\S*"
whitespace = ~"\s+"
comment = ~"^#.*$"
